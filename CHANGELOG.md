# 변경 이력

정식 버전은 승인된 코드가 `main`에 반영되고 프로덕션에 배포될 때만 기록합니다. 기능 브랜치의 프리뷰 수정과 폐기된 실험에는 별도 버전을 부여하지 않습니다.

## v2.2.2 — 참고값 갱신 안내 정리

- 프리미엄과 온체인 수수료 사이에 있던 공통 갱신 안내를 제거하고, 각 값 가까이에 `약 5분마다 자동 갱신`과 최근 확인 시각을 각각 표시하도록 정리했습니다.
- 시장 조회 주기와 WebSocket·REST fallback 동작은 v2.2.1과 동일하게 유지했습니다.

## v2.2.1 — 시장 조회 사용량 최적화

- BTC/KRW 실시간 가격은 기존처럼 업비트 WebSocket으로 수신하고, 정상 연결 중 프리미엄과 mempool.space 수수료의 REST 자동 조회 주기를 5분으로 조정했습니다.
- WebSocket 장애 시 BTC/KRW 가격을 보완하는 REST 조회는 60초 주기로 유지했습니다.
- WebSocket 재연결은 15초, 30초, 60초 순서로 간격을 늘리고 이후에는 60초 간격을 유지하도록 변경했습니다.
- 최초 진입·화면 복귀·온라인 복구·수동 새로고침에서는 즉시 조회하며, 수동 조회가 끝난 시점부터 다음 자동 조회 주기를 다시 계산합니다.
- 화면에 프리미엄과 온체인 수수료의 5분 자동 갱신 정책을 안내하고, 백그라운드·오프라인에서는 조회와 재연결을 중지합니다.

## v2.2.0 — 거래 기록 카드와 결제정보 공유 개편

- 공유 영역을 `상대 찾기·공유하기`로 묶고, 공개 채널용 `모집글`과 합의 후 사용하는 `거래 기록 카드`의 목적을 분리했습니다.
- 거래 기록 카드를 만들 때 조건을 서버에서 다시 확인하고 기록 ID·생성 시각·상세정보 링크를 발급하는 서명 기록 흐름을 추가했습니다.
- 상세정보 페이지에서 공유된 조건과 결제정보를 확인하고 모바일에서도 주소나 인보이스만 따로 복사할 수 있도록 했습니다.
- 공개 모집글에서는 실제 구매 자금 출처와 결제정보를 제외하고, 거래 기록 카드에서만 선택적으로 포함하도록 범위를 정리했습니다.
- 온체인 주소는 주소만 포함하거나 현재 거래 금액이 담긴 BIP21 QR로 만들 수 있게 했으며, 붙여 넣은 BIP21의 금액 일치 여부도 확인합니다.
- Lightning Address/LNURL-pay 주소만 기록하는 흐름과 결제 직전 고정금액 BOLT11 인보이스를 생성하는 흐름을 분리했습니다.
- 결제정보가 있으면 해당 결제 QR을, 없으면 거래 기록을 다시 열 수 있는 보관용 링크 QR을 카드에 표시하도록 구분했습니다.
- 중복되던 수취정보 공유 버튼·대형 QR 미리보기·인보이스 전용 공유 흐름을 제거하고 `거래 기록 카드 공유`로 통합했습니다.
- 카드 QR과 구분선의 여백, 공유 탭의 모바일 줄바꿈, 결제정보 결과 패널과 주요 안내 문구를 일관되게 다듬었습니다.
- 휴대폰에서는 거래 금액과 판매자 프리미엄 입력을 위아래로 배치하고, 넓은 화면에서는 두 입력의 높이와 비율을 맞췄습니다.
- 기록 생성 요청 제한, 입력 크기·스키마 검사, 업비트 시세 독립 확인, 제한된 리디렉션 처리와 변조 검사를 추가했습니다.

## v2.1.2 — 공유·설치·하단 UI 안정화

- 거래 조건 4:3 공유 이미지를 기존 1600×900 PNG를 먼저 만든 뒤 다시 변환하는 방식에서, 최종 4:3 이미지를 직접 생성하는 방식으로 바꿔 불필요한 렌더링과 메모리 사용을 줄였습니다.
- 직접 4:3 변환이 불가능한 환경에서는 기존 PNG 생성 경로를 fallback으로 유지해 호환성을 보존했습니다.
- Samsung Internet에서는 불안정했던 직접 설치 프롬프트 대신 실제 확인된 Android Chrome 설치 안내로 유도하도록 변경했습니다.
- `계산 기준·데이터 출처`, `제작자 보기`, 하단 푸터 사이 여백을 각각 16px 기준으로 통일해 과도한 공백과 붙어 보이는 구간을 함께 정리했습니다.
- 위 공유·설치·하단 배치가 다시 퇴행하지 않도록 회귀 테스트를 추가했습니다.

## v2.1.1 — 계산기 하단 여백 조정

- 계산기 하단 참고 영역과 푸터 사이의 과도한 공백을 줄여 화면 하단 밀도를 조정했습니다.

## v2.1.0 — BTC 수취정보와 거래조건 공유 개편

- 판매자 프리미엄 기본값을 2%에서 0%로 변경했습니다.
- 거래 조건 이미지에서 선택 사항으로 BTC 받을 정보를 입력할 수 있도록 하고, 온체인 주소·Lightning Address/LNURL-pay·BOLT11 인보이스를 구분해 처리합니다.
- Lightning Address의 표준 LNURL-pay 흐름을 이용해 거래 금액에 맞는 BOLT11 인보이스를 자동 발급하고, 발급된 인보이스의 네트워크·금액·만료·서명 등을 검증합니다.
- 온체인 주소는 BIP21 금액 지정 요청 QR을 만들 수 있으며, Lightning 인보이스와 함께 QR 중앙에 엄지왕 로고를 넣어 공유할 수 있습니다.
- 주소·인보이스 입력칸에 붙여넣기 버튼을 추가해 모바일에서 긴 문자열 입력을 단순화했습니다.
- 거래조건 공유 이미지를 4:3 카드로 개편하고 우측에 실제 결제 QR을 크게 배치했습니다. QR이 없으면 입력한 수취정보를 대신 표시합니다.
- `함께 공유되는 문구` 첫 줄에 `P +3%` 같은 프리미엄을 표시하고, BTC 수취정보를 QR 생성 여부와 관계없이 자동 포함합니다.
- 가격 계산 근거는 기본 공유문구에서 숨기고 `상세 계산정보 포함`을 선택했을 때만 계산 시각·기준 시세·적용 단가·시장 참고값을 추가하도록 단순화했습니다.
- 거래 수수료 부담 표현은 결제 방식에 관계없이 `수수료: 판매자 부담`으로 통일했습니다. 온체인 네트워크 수수료율 자체는 기존처럼 mempool.space 참고값으로 표시합니다.
- 제작자·후원 영역을 `계산 기준·데이터 출처`와 같은 접이식 카드 구조로 정리하고 모바일 여백과 테두리를 통일했습니다.
- PWA 설치 안내와 수취정보·공유 UI를 모바일 환경에 맞게 다듬고, 실험 과정에서 남은 중복·구형 흐름을 정리했습니다.

## v2.0.8 — 런타임·PWA 최적화 마무리

- 서비스 워커 등록 URL과 캐시 이름을 앱 버전에 연결해 새 릴리스에서 오프라인 앱 셸까지 확실히 교체되도록 변경했습니다.
- 서비스 워커의 오프라인 fallback은 현재 앱 버전의 precache·runtime cache만 사용하고, 이전 버전 캐시는 활성화 단계에서 정리하도록 강화했습니다.
- 업비트 WebSocket을 `ticker` 전체 응답에서 `trade + SIMPLE + realtime only` 구독으로 바꿔 실시간 가격 수신 데이터량과 JSON 처리량을 줄였습니다.
- 거래 모집글이 숨겨진 상태에서는 실시간 시세 변화만으로 다시 렌더링하지 않도록 메모이제이션하되, 사용자가 작성한 모집글 상태는 그대로 보존합니다.
- 업비트 프리미엄에 60초 독립 캐시, 실패 시 짧은 재시도 백오프, 동시 요청 병합을 추가해 동일 외부 조회를 줄였습니다.
- 후원 영역 전체의 클라이언트 하이드레이션을 없애고 라이트닝 주소 복사 버튼만 클라이언트 컴포넌트로 분리했습니다.
- 더 이상 사용하지 않는 구형 Apple 터치 아이콘과 일부 불필요한 공개 아이콘 원본을 정리했습니다.
- README의 운영 버전과 실시간 시세·PWA 캐시 설명을 현재 구현에 맞춰 갱신했습니다.

## v2.0.7 — 유지보수·검증 최적화

- 설치 안내의 iPhone·Android 이미지를 지연 로딩하고 비동기 디코딩하도록 변경했습니다.
- 실시간 가격 연결 상태에 따른 시장 갱신 간격 계산을 공용 함수로 통합하고, 운영 코드와 테스트가 같은 로직을 검증하도록 정리했습니다.
- 거래 모집글 미리보기의 불필요한 상태 초기화 렌더를 줄이고, 직접 편집한 문구 보존과 자동 갱신 로직을 공용 함수로 연결했습니다.
- 앱 버전 테스트의 고정 버전 문자열을 제거하고 `APP_VERSION`과 `package.json` 버전이 일치하는지 검증하도록 변경했습니다.
- `verify` 명령으로 린트·프로덕션 빌드·빌드 결과 테스트를 한 번에 수행하고, 수동 배포도 검증 성공 후에만 실행하도록 정리했습니다.
- Pull Request와 `main` 변경을 검사하는 GitHub Actions 검증을 추가하고, Tailwind 제거 후 남아 있던 잠금 파일을 현재 의존성 기준으로 정리했습니다.

## v2.0.6 — 빌드 의존성 정리

- 실제 UI에서 사용하지 않던 Tailwind CSS와 `@tailwindcss/postcss` 직접 의존성을 제거했습니다.
- Tailwind를 위해 존재하던 PostCSS 설정을 제거해 CSS 빌드 구성을 단순화했습니다.
- 기존 화면의 기본 스타일 동작을 유지하도록 필요한 Preflight 규칙을 프로젝트 내부 CSS로 고정했습니다.
- 계산기, 실시간 시세, 공유, Worker 로직은 변경하지 않았습니다.

## v2.0.5 — 가격 갱신 렌더링 최적화

- 실시간 가격이 바뀔 때 공유 이미지용 임시 File과 관련 상태를 미리 만들던 흐름을 제거했습니다.
- 거래 조건 이미지 모듈과 공유 데이터 준비는 사용자가 `거래 조건 이미지 공유` 버튼을 누른 뒤에만 실행합니다.
- 거래 모집글의 자동 미리보기는 시세 환산값 변경을 같은 렌더에서 바로 반영해 불필요한 추가 렌더를 줄였습니다.
- 사용자가 직접 편집한 모집글은 가격 변화만으로 덮어쓰지 않도록 기존 동작을 유지했습니다.

## v2.0.4 — 백그라운드 실시간 처리 중지

- 페이지가 백그라운드로 이동하면 업비트 WebSocket 연결, 재연결 예약, 대기 중인 실시간 가격 반영과 1초 시계 타이머를 즉시 중지합니다.
- 다시 화면으로 돌아오면 WebSocket을 즉시 재연결해 최신 체결가 수신을 재개합니다.
- 오프라인 상태에서는 WebSocket 재연결을 시도하지 않고, 온라인 복귀 후 화면이 보이는 경우에만 연결을 복구합니다.
- 화면이 보이고 온라인인 상태에서 WebSocket 연결이 끊기면 12초마다 재연결을 시도합니다.

## v2.0.2 — 공유 이미지 생성 최적화

- 실시간 시세가 바뀔 때마다 1600×900 거래 조건 PNG를 백그라운드에서 반복 생성하던 동작을 제거했습니다.
- 평상시에는 공유에 필요한 거래 조건 데이터만 가볍게 준비하고, 실제 캔버스 렌더링과 PNG 인코딩은 사용자가 `거래 조건 이미지 공유` 버튼을 누른 순간 한 번만 수행합니다.
- PNG 생성 후 기존과 동일하게 기기의 공유 기능을 열고, 파일 공유를 지원하지 않는 환경에서는 PNG 저장으로 대체합니다.

## v2.0.0 — 거래 모집과 조건 공유

- `거래 모집글`과 `거래 조건 이미지` 중 만들 결과를 선택할 수 있습니다.
- 구매·판매, 원화·sats·BTC 금액, 판매자 프리미엄, 온체인·라이트닝 전송 방식을 모집글로 정리합니다.
- 기존 거래자 우대 프리미엄, 원화 출처 설명 가능, 상호 신원확인 협의 가능, 추가 메모를 선택적으로 넣을 수 있습니다.
- 모집글 미리보기를 직접 편집하고 기기의 공유 기능으로 전달할 수 있습니다.
- 모집글은 선택한 금액 단위를 기준으로 표시하고, 다른 단위는 읽기 쉬운 약값 하나만 덧붙입니다.
- 거래 조건 이미지의 비트코인 표시 단위를 통일하고 공유 링크의 목적을 `거래 조건 검증`으로 분명히 했습니다.
- 공개 모집글에는 실제 자금 출처 종류, 주소, 인보이스, QR, 지급 요청을 포함하지 않습니다.

## v1.4.0 — 2.0 이전 운영판

- 금액·프리미엄 입력 조작과 실시간 시계 표시를 다듬었습니다.

## v1.3.0 — 업비트 실시간 시세

- 업비트 WebSocket BTC/KRW 최근 체결가와 REST 장애 대응을 추가했습니다.

## v1.2.0 — 사용성 및 공유 안정화

- 거래 초안 보존, 공유 화면, 설치 안내, 시장·수수료 갱신을 개선했습니다.

## v1.1.0 — 거래 금액 단위 확장

- 원화·sats·BTC 기준 계산과 구매·판매 흐름을 개선했습니다.

## v1.0.0 — 최초 공개판

- P2P 계산, 온체인 수수료, 거래 조건 공유, PWA 설치를 처음 공개했습니다.
